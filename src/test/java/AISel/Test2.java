package AISel;

import org.openqa.selenium.firefox.FirefoxDriver;

public class Test2 {

	public static void main(String[] args) {
		
		//Open firefox browser
		FirefoxDriver d=new FirefoxDriver(); //create an object to a class using its constructor Method 
		//SearchContext driver=new FirefoxDriver();//Create an object to interface using concrete class's constructor
		//WebDriver driver=new FirefoxDriver();  //Create an Object to a class using child class constructor method
	    //RemoteWebDriver driver=new FirefoxDriver(); //Create an object to a concrete class constructor method.

	}

}
